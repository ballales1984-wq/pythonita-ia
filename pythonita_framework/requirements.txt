# Pythonita AI Agent Framework
# Requisiti per il framework multi-agent offline

# Core
# (solo stdlib - nessuna dipendenza obbligatoria)

# Optional - per Vector Memory (RAG)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Optional - per LLM locale
# ollama (installato separatamente)

# Optional - per testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Per sviluppo
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0